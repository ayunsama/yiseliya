# 创意工坊 MOD 编写指南

## 什么是 MOD？

MOD 是一组文件的集合，可以被玩家一键安装到角色卡系统中。
MOD 可以新增职业、种族、开局、英灵、DLC、物品、独立脚本或界面。

## 文件结构

一个 MOD 就是一个文件夹，包含以下文件：

```
你的MOD名/
├── manifest.json   ← 必填：MOD 的基本信息
└── 数据文件         ← 根据 MOD 类型不同（见下方说明）
```

---

## manifest.json 模板

```json
{
  "名称": "龙骑士职业",
  "作者": "你的名字",
  "版本": "1.0.0",
  "类型": "职业",
  "描述": "新增强力龙骑士职业，重甲龙族战士"
}
```

**类型** 填以下之一：

| 类型 | 说明 | 需要的数据文件 |
|------|------|---------------|
| `职业` | 新增一个可选的职业 | `growth.json` + `skills.yaml` |
| `种族` | 新增可选种族（含属性修正） | `races.json` |
| `开局` | 新增初始开局方案 | `scenarios.json` |
| `英灵` | 新增英灵类型 | `spirits.json` |
| `DLC` | 新增资料片/IF线 | `dlc.json` |
| `物品` | 新增物品到背包 | `items.json` |
| `脚本` | 独立运行的后台脚本 | `script.js` |
| `界面` | 独立显示的前端界面 | `interface.html` |

---

## 各类 MOD 的编写方法

### 类型 = 职业

需要两个文件：

**growth.json** — 职业成长数据：

```json
{
  "职业名": "龙骑士",
  "HP骰": "d10",
  "SP骰": "d6",
  "MP骰": "d4",
  "每级技能点": 4,
  "关键属性": ["力量", "魅力"],
  "转职条件": "力量≥14，魅力≥13",
  "职业特色": "骑乘龙族伙伴作战，龙息强化近战"
}
```

- `HP骰`、`SP骰`、`MP骰` 填骰面格式如 `d6`、`d10`、`d8+1`
- 没有该资源就填空字符串 `""`（如无 MP 的职业就把 MP 骰留空）
- `每级技能点` 整数，决定了每升一级获得多少技能点去学技能

**skills.yaml** — 技能树（可选，只用于展示）：

```yaml
龙骑士:
  HP骰: d10
  SP骰: d6
  MP骰: d4
  技能点: 每级4点
  关键属性: 力量与魅力
  转职条件: 力量≥14且魅力≥13
技能树:
  Lv1（1点/个）:
    龙牙突刺: "SP1 | 近战附加1d6穿刺伤害"
    龙鳞护体: "[被动] 基础防御+2"
    龙吼: "SP2 | 敌人意志豁免DC13失败则恐慌1轮"
  Lv3（2点/个，需职业等级≥3）:
    龙息斩: "SP3 | 近战附加2d6火焰伤害"
    龙翼滑翔: "SP2 | 飞行至R20尺内任一位置"
```

格式完全对齐现有的职业技能树写法。

---

### 类型 = 种族

需要 **races.json**：

```json
[
  {
    "种族名": "龙裔",
    "描述": "流淌着龙族血脉的古老种族，天生拥有强壮的体魄和火焰亲和",
    "属性修正": { "力量": 2, "体质": 1, "魅力": 1 },
    "可选职业": ["战士", "骑士", "狂战士"]
  }
]
```

- `属性修正` 中的 key 为六维属性名（力量/敏捷/体质/智力/感知/魅力），value 为加减值
- `可选职业` 限制该种族可选的职业，不填则无限制

---

### 类型 = 开局

需要 **scenarios.json**：

```json
[
  {
    "开局名": "龙裔流浪者",
    "描述": "你是一条被族群放逐的年轻龙裔，带着祖传的龙鳞碎片踏上旅程",
    "初始地点": "龙骸荒原",
    "初始物品": ["破损的长剑", "龙鳞碎片", "旧行囊"],
    "初始职业": ["战士"],
    "初始属性": { "力量": 14, "体质": 13, "敏捷": 10, "智力": 10, "感知": 10, "魅力": 8 },
    "初始任务": ["寻找龙族遗迹"]
  }
]
```

- 所有字段都是可选的，不填会用默认值

---

### 类型 = 英灵

需要 **spirits.json**：

```json
[
  {
    "名称": "龙魂守护者",
    "描述": "古代龙族英雄的残魂，能赐予宿主龙鳞般的防御力",
    "初始残响之力": 20,
    "初始状态": "苏醒"
  }
]
```

---

### 类型 = DLC

需要 **dlc.json**：

```json
{
  "世界覆盖": {
    "紧张度": { "当前值": 80, "等级": "决战期" },
    "当前地点": "龙骸荒原"
  },
  "变量更新规则补充": "龙裔种族相关规则：龙裔在火山/熔岩地形中力量检定+2"
}
```

- `世界覆盖` 会在 DLC 启用时覆盖世界字段的初始值
- `变量更新规则补充` 是追加到世界书变量更新规则中的文本

---

### 类型 = 物品

需要 **items.json**：

```json
[
  {
    "物品名": "龙鳞重甲",
    "物品大类": "防具",
    "品阶": "稀有",
    "具体类别": "重甲",
    "装备位": "防具",
    "基础属性": {
      "防御值": "+4",
      "材质": "龙鳞",
      "敏捷上限": "2",
      "移动惩罚": "无"
    },
    "词条与机制": {
      "词条效果": {
        "火焰抗性": "+20%"
      }
    },
    "交易与物理属性": {
      "数量": 1,
      "价格": "500金"
    },
    "描述": "以龙鳞锻造的全身重甲"
  }
]
```

`物品大类` 可选：`武器`、`防具`、`饰品`、`消耗品`、`材料`、`一般物品`
`品阶` 可选：`普通`、`精良`、`稀有`、`史诗`、`传说`、`神话`、`唯一`

---

### 类型 = 脚本

需要 **script.js** — 任意纯 JavaScript 文件。

脚本会被 `import()` 动态加载执行，可以：

```javascript
// script.js 示例
console.log('[我的MOD] 脚本已加载');
toastr.success('我的MOD脚本已运行');
```

脚本中可以使用酒馆助手的全部 API（getVariables、eventOn 等）。

---

### 类型 = 界面

需要 **interface.html** — 任意 HTML 文件。

界面会被插入到酒馆页面中显示，可以包含 CSS 样式和 JS 脚本。

---

## 如何上传

### 方式一：通过 GitHub（推荐）

1. 打开创意工坊仓库的 GitHub 页面
2. 进入 `packages/` 文件夹
3. 点右上角 **Add file → Upload files**
4. 把你写好的整个文件夹拖进去
5. 填写 commit 信息，选 **Create a new branch and start a pull request**
6. 提交后等待审核

### 方式二：直接发文件

把 MOD 文件夹打包成 zip，发给维护者。

---

## 常见问题

**Q: 我的 MOD 和已有的职业重名了怎么办？**
A: 不要重名。如果一定要覆盖已有的职业，联系维护者说明。

**Q: 我的 MOD 可以依赖另一个 MOD 吗？**
A: 当前版本不支持依赖管理，建议在描述中说明前置条件。

**Q: 如何测试我的 MOD？**
A: 把文件传到任意 HTTP 服务器（GitHub raw、jsDelivr 等），然后在创意工坊脚本中临时修改 `WORKSHOP_URL` 指向你自测用的 `packages.json`。
